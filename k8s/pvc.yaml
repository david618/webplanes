apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: planes
  namespace: simulators
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-premium
  resources:
    requests:
      storage: 512Gi